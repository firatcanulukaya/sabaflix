// import {} from "../types";

const initial_state = {
    serverLink: "http://10.80.0.168:8080"
}

const reducer = (state = initial_state, action) => {
    switch (action.type) {
        default:
            return state
    }
}

export default reducer